alias e="open -a /Applications/Emacs.app"
alias rmr="rm -rf"
alias viconf="vi ~/.vimrc"
alias zshconf="vi ~/.zshrc.mine"
alias less="less -N -M"

# global aliases
alias -g L="|less"
alias -g G="|grep"
alias -g XG="|xargs grep -Hn"
alias -g XG0="|xargs -0 grep -Hn"

function grepr() {
   echo "find . -name \"$2\" | xargs grep $1"
}

path=(. $HOME/bin /usr/local/sbin /usr/local/bin /sbin /bin /usr/sbin /usr/bin /usr/games /usr/X11R6/bin $HOME/bin/depot_tools)

# ignore case for completion
zstyle ':completion:*' matcher-list 'm:{a-z}={A-Z}'

# prompt
PROMPT="%{[35m%}%U$USER@%m%u%%%{[m%} "
RPROMPT='%{[34m%}[%~]%{[m%}'

